// Optional deploy-time default for the backend API base URL.
//
// Leave it empty to require the user to enter the URL in the UI (it is then
// remembered in localStorage). Or set it here before deploying the frontend
// so the page connects automatically, e.g.:
//
//   window.__API_BASE__ = "https://your-backend.onrender.com";
//
window.__API_BASE__ = "";
