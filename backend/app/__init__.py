"""Transaction & Ranking backend service.

A small FastAPI service that accepts user transactions, exposes per-user
summaries, and produces a fair, multi-factor ranking (leaderboard).

See ``app.main`` for the API surface and the project README for the full
design write-up.
"""

__version__ = "1.0.0"
